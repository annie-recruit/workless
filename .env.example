# ==========================================
# Workless 환경변수 예제
# ==========================================
# 이 파일을 .env.local로 복사하여 실제 값을 입력하세요
# ==========================================

# ==========================================
# NextAuth (인증)
# ==========================================
# NextAuth URL (프로덕션: https://workless.me, 로컬: http://localhost:3000)
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret (랜덤 문자열 생성: openssl rand -base64 32)
NEXTAUTH_SECRET=your-nextauth-secret-here

# ==========================================
# Google OAuth (로그인)
# ==========================================
# Google Cloud Console에서 발급
# OAuth 2.0 클라이언트 ID 생성 필요
# 리디렉션 URI: https://workless.me/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ==========================================
# OpenAI (AI 기능)
# ==========================================
# OpenAI Platform에서 발급
# AI 요약, 그룹화, 인사이트 기능에 필요
OPENAI_API_KEY=sk-proj-...

# ==========================================
# Workless API Keys
# ==========================================
# iOS Shortcut, Raycast, CLI 등 외부 클라이언트용
WORKLESS_SEND_API_KEY=31ac244bf8c32ca4e4f2a05f4dfcc1fae1ab4d74c9d8801952ecfb40df489a9d

# Railway/프로덕션 환경용
WORKLESS_API_KEY=your-workless-api-key-here

# ==========================================
# Sentry (에러 트래킹) - 선택사항
# ==========================================
# Sentry.io에서 프로젝트 생성 후 DSN 복사
# 클라이언트(브라우저) 전송용
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# 서버/엣지/Route Handler 전송용
SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# 성능 트레이싱 샘플링 비율 (0~1)
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_TRACES_SAMPLE_RATE=0.1

# 환경 설정
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development
SENTRY_ENVIRONMENT=development

# 릴리즈 버전 (Git commit hash 등)
NEXT_PUBLIC_SENTRY_RELEASE=
SENTRY_RELEASE=

# Sentry 소스맵 업로드용 (CI/CD에서 사용)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=your-org-name
SENTRY_PROJECT=workless

# Sentry Webhook 서명 검증용 시크릿
SENTRY_WEBHOOK_SECRET=your-webhook-secret

# ==========================================
# Adobe PDF Services - 선택사항
# ==========================================
# Adobe PDF Services API 사용 시 필요
# PDF 처리, 변환, OCR 기능에 사용
PDF_SERVICES_CLIENT_ID=your-pdf-services-client-id
PDF_SERVICES_CLIENT_SECRET=your-pdf-services-client-secret

# ==========================================
# PixelLab API - 선택사항
# ==========================================
# 픽셀 아트 생성 기능에 사용
PIXELLAB_API_KEY=your-pixellab-api-key

# ==========================================
# Railway 환경 (자동 설정됨)
# ==========================================
# Railway에서 자동으로 설정되는 변수들
# RAILWAY_ENVIRONMENT=production
# RAILWAY_VOLUME_MOUNT_PATH=/data
