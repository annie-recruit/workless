#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 타입 체크만 실행 (빌드 실패 방지)
echo "🔍 Running type check..."
npm run type-check || exit 1

echo "✅ Pre-commit checks passed!"
