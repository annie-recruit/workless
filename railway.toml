[build]
builder = "NIXPACKS"

[deploy]
numReplicas = 1
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# SQLite 데이터베이스 영구 저장을 위한 볼륨 마운트
[[deploy.volumeMounts]]
mountPath = "/app/data"
